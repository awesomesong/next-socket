"use client";

import useActiveChannel from "@/src/app/hooks/useActiveChannel";
import useActiveUser from "@/src/app/hooks/useActiveUser";

const UserActiveStatus = () => {
    // useActiveChannel();
    useActiveUser();
    
    return null;
};

export default UserActiveStatus;